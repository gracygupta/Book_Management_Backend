<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
      crossorigin="anonymous"
    />
  </head>
  <body style="height: 100vh; width: 100vw">
    <div
      style="
        width: 100%;
        height: 100vh;
        background-color: #130f40;
        overflow: scroll;
      "
    >
      <div
        style="
          padding: 40px;
          display: flex;
          flex-wrap: wrap;
          flex-direction: row;
          justify-content: center;
          align-items: center;
        "
      >
        <% if (books.length){%><% for (let book of books) { %>
        <div
          class="card"
          style="width: 18rem; background-color: #dedede; margin: 10px"
        >
          <div class="card-body" style="height: 100%">
            <h5
              class="card-title"
              style="height: 4rem; width: 100%; color: black"
            >
              <%- book.name %>
            </h5>
            <h6 class="card-subtitle mb-2 text-muted">
              Author: <%- book.author_name %>
            </h6>
            <h6 class="card-subtitle mb-2 text-muted">
              Genre: <%- book.genre %>
            </h6>
            <h6 class="card-subtitle mb-2 text-muted">
              Book Number: <%- book.isbn_no %>
            </h6>
            <h6 class="card-subtitle mb-2 text-muted">
              Inventory: <%- book.inventory %>
            </h6>
          </div>
        </div>
        <% } %><% } else { %>
        <h4 style="height: 2px; color: white">No Books to Show</h4>
        <% } %>
      </div>
    </div>
  </body>
</html>
